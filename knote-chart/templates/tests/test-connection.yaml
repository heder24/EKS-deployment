apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knote-chart.fullname" . }}-test-connection"
  labels:
    {{- include "knote-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knote-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
